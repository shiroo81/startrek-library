/// <reference types="cypress" />

describe("Given there is a Star Wars API provided", () => {
    describe("When I search for spaceships", () => {
      // BONUS ASSIGNMENT:
      // Use cy.request to request the following URL: https://swapi.dev/api/starships/9/
      // Give it an alias and assert:
      // - response status is 200,
      // - hyperdrive_rating is 4.0
      // Tip: Use cy.log to visually check the response data.
      it(`should verify the Death Star Hyperdrive`, () => {
        


      });
    });
  });
  